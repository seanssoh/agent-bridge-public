# scripts/baselines/iso-helper-allowlist.txt
#
# Whole-file allowlist for scripts/iso-helper-ratchet.sh. Lines starting
# with `#` are comments. One file relpath per line (relative to repo
# root). Files listed here are compatibility wrappers or controller-only
# code that the ratchet explicitly does NOT enforce on.
#
# The always-allowlisted internal files (helper itself, smoke, ratchet,
# Python adapter) are hard-coded in the ratchet script and do NOT
# need to be listed here.
#
# Add a file here ONLY if you have audited its mentions and confirmed
# they are either (a) inside a function that already delegates through
# bridge_iso_run, or (b) controller-only by design (operator-supplied
# token files, controller-owned channels-home dotenv, in-process state
# files that never escape into an isolated tree).

# Compatibility wrappers that already delegate to bridge_iso_run /
# bridge_iso_paths.iso_run (callers are migrated, body remains for
# back-compat with operator-installed shims).

# PR-C (#1520c) test fixtures — reference iso-boundary filenames (.env, profile files,
# HEARTBEAT.md) only to ASSERT publish/exclusion contracts; not runtime boundary RW.
scripts/smoke/1520c-create-isolate-profile-publish.sh
scripts/smoke/1520c-create-isolate-helpers/assert-profile-publish-modes.py
scripts/smoke/1520c-create-isolate-helpers/assert-watchdog-publish-gap.py

# #1533 test fixtures — reference iso-boundary filenames (.env, agent-env.sh,
# HEARTBEAT.md) only to ASSERT the content-tree publish/exclusion contracts in a
# mktemp fixture; not runtime boundary RW.
scripts/smoke/1533-create-isolate-content-publish.sh
scripts/smoke/1533-create-isolate-helpers/assert-content-tree-modes.py

# #1766 test fixture — references `settings.effective.json` / `settings.json`
# only to ASSERT the iso-settings group-publish (0640/0750) + the content-tree
# walker's target-validated symlink accept/refuse contract + the picker-catalog
# Settings-Error fingerprint, all in mktemp fixtures; not runtime boundary RW
# (same shape as the #1520c / #1533 test-fixture allowlist entries). The two
# 1766-iso-settings-helpers/*.py assertion helpers carry NO bare boundary
# filenames under the anchored #1764 pattern, so they need no allowlist entry.
scripts/smoke/1766-iso-settings-readable.sh

# #1563 PR-5 integration matrix — a heartbeat state-file STUB (`heartbeat/<a>.env`)
# inside a mktemp BRIDGE_HOME only mirrors the PR-3 escalation predicate's input to
# ASSERT the admin-liveness false-positive control; it never RWs a controller->iso
# boundary path (same shape as the #1520c / #1533 test-fixture allowlist entries).
scripts/smoke/1563-pr5-fp-control-matrix.sh

# #1563 PR-8 diag/recovery fixture — the helper only sets process-local env vars
# (`os.environ["BRIDGE_A2A_OUTBOX_DB"|"BRIDGE_A2A_CONFIG"]`, `dict(os.environ)`) to
# drive `agb a2a` / daemon-helper subprocesses inside a mktemp WORK dir; the `.env`
# matches are the `environ` substring, never a controller->iso boundary RW (same shape
# as the #1520c / #1533 / PR-5 test-fixture allowlist entries).
scripts/smoke/1563-pr8-a2a-diag-recovery-helper.py

# #1685 receiver-staleness fixture — the only `.env` match is the daemon-owned
# cadence file `state/handoff/receiver-staleness-tick.env` (mirrors
# receiver-supervise-tick.env), cleared inside a literal-heredoc runner under a
# mktemp WORK dir; it is controller-side daemon state, never a controller->iso
# boundary RW (same shape as the #1520c / #1533 / PR-5 / PR-8 fixture entries).
scripts/smoke/1685-receiver-staleness-selfheal.sh

# #1728 test-bind state-path guard fixture — the helper ONLY sets process-local
# env vars (`os.environ["BRIDGE_A2A_ALLOW_TEST_BIND"|"BRIDGE_STATE_DIR"|
# "BRIDGE_A2A_{OUTBOX,INBOX,ROOMS,RECONCILE}_DB"]`, `os.environ.pop(...)`) to
# drive the real A2A/rooms/reconcile open choke points against ISOLATED mktemp
# dirs; every `.env` match is the `os.environ` substring, never a controller->iso
# boundary RW (same shape as the #1520c / #1533 / PR-5 / PR-8 fixture entries).
scripts/smoke/1728-test-bind-state-path-guard-helper.py
